# --- Provider API Keys ---
# Key for the provider(s) configured in config.toml.
# Convention: <PROVIDER_NAME>_API_KEY (e.g. groq → GROQ_API_KEY)
GROQ_API_KEY=<your-groq-api-key>
GOOGLE_API_KEY=<your-google-api-key>
INCEPTION_API_KEY=<your-inception-api-key>

# --- Sandbox (optional) ---
# Only required if [sandbox] is configured in config.toml
DENO_API_KEY=<your-deno-api-key>

# --- Sandbox Dependency Secrets (optional) ---
# API keys for packages listed under [[sandbox.dependencies]]
LINEAR_API_KEY=<your-linear-api-key>
